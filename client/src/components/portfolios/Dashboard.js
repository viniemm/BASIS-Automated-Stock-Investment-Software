import React, { Fragment } from 'react';
import Portfolios from './Portfolios';

export default function Dashboard() {
    return (
        <Fragment>
            <Portfolios />
        </Fragment>
    )
}